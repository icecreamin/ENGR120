raise arm after connection is done during exit. Also exit code needs fixing. it doesnt always exit away.

the robot keeps missing the beacon. possibly add back in contant check if it can see the beacon.
